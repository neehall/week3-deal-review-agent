OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Optional: enables LangSmith tracing automatically when set. Leave blank to
# skip LangSmith -- the pipeline always writes local JSONL trace/audit files
# regardless of this being set.
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=week3-deal-review-agent
